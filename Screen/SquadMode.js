import { StyleSheet, Text, View } from "react-native";
import React from "react";

const SquadMode = () => {
  return (
    <View>
      <Text>SquadMode</Text>
    </View>
  );
};

export default SquadMode;

const styles = StyleSheet.create({});
